diff --git a/mmCoreAndDevices/DeviceAdapters/Makefile.am b/mmCoreAndDevices/DeviceAdapters/Makefile.am
index 1234567..89abcde 100644
--- a/mmCoreAndDevices/DeviceAdapters/Makefile.am
+++ b/mmCoreAndDevices/DeviceAdapters/Makefile.am
@@ -5,7 +5,8 @@
 # Order matters: The adapters listed earlier are loaded first by the DeviceManager
 # (This list is processed by configure.ac, which adds them to build configuration)
 SUBDIRS = DemoCamera \
-        # BlueboxOptics_niji DISABLED: Boost lexical_cast issue on macOS ARM64 \
+        # BlueboxOptics_niji DISABLED: Boost lexical_cast issue on macOS ARM64 \
+        Spinnaker4 \
         $(DEMO_ADAPTERS) \
         $(PLUGIN_ADAPTERS) \
         $(PROP_ADAPTERS)

diff --git a/mmCoreAndDevices/DeviceAdapters/configure.ac b/mmCoreAndDevices/DeviceAdapters/configure.ac
index 1234567..89abcde 100644
--- a/mmCoreAndDevices/DeviceAdapters/configure.ac
+++ b/mmCoreAndDevices/DeviceAdapters/configure.ac
@@ -142,7 +142,9 @@ if test "x$build_plugins" = xyes; then
   m4_define([PCO_UCT], [deviceadapter/PCO_UCT/Makefile.am])
   m4_define([PCO_SC2], [deviceadapter/PCO_SC2/Makefile.am])
   m4_define([PCO_USB], [deviceadapter/PCO_USB/Makefile.am])
   # m4_define([BlueboxOptics_niji], [deviceadapter/BlueboxOptics_niji/Makefile.am]) DISABLED: Boost lexical_cast issue on macOS ARM64
+  m4_define([Spinnaker4], [deviceadapter/Spinnaker4/Makefile.am])
   m4_define([LeicaDMi8], [deviceadapter/LeicaDMi8/Makefile.am])
   m4_define([Lumenera], [deviceadapter/Lumenera/Makefile.am])
   m4_define([ASI], [deviceadapter/ASI/Makefile.am])

@@ -900,6 +902,42 @@ MM_INSTALL_DIRS

 ##
 ## Subdirectory configuration
 ##

+##
+## Spinnaker SDK 4.x.x detection and configuration
+##
+
+# Spinnaker SDK 4.x.x detection
+AC_ARG_WITH([spinnaker],
+    [AS_HELP_STRING([--with-spinnaker=DIR],
+        [Spinnaker SDK 4.x.x installation directory])],
+    [SPINNAKER_DIR="$withval"],
+    [SPINNAKER_DIR=""])
+
+if test -n "$SPINNAKER_DIR"; then
+    SPINNAKER_INCLUDE_DIR="$SPINNAKER_DIR/include"
+    SPINNAKER_LIB_DIR="$SPINNAKER_DIR/lib"
+
+    # Check for Spinnaker headers
+    AC_MSG_CHECKING([for Spinnaker SDK 4.x.x headers])
+    AC_CHECK_FILE([$SPINNAKER_INCLUDE_DIR/Spinnaker.h],
+        [AC_MSG_RESULT([found in $SPINNAKER_INCLUDE_DIR])],
+        [AC_MSG_ERROR([Spinnaker SDK 4.x.x headers not found in $SPINNAKER_INCLUDE_DIR])])
+
+    # Check for Spinnaker library
+    AC_MSG_CHECKING([for Spinnaker SDK 4.x.x libraries])
+    AC_CHECK_FILE([$SPINNAKER_LIB_DIR/libSpinnaker.dylib],
+        [SPINNAKER_LIBS="-L$SPINNAKER_LIB_DIR -lSpinnaker"
+         AC_SUBST([SPINNAKER_CPPFLAGS], ["-I$SPINNAKER_INCLUDE_DIR"])
+         AC_SUBST([SPINNAKER_LIBS], ["$SPINNAKER_LIBS"])
+         AC_MSG_RESULT([found in $SPINNAKER_LIB_DIR])],
+        [AC_MSG_ERROR([Spinnaker SDK 4.x.x libraries not found in $SPINNAKER_LIB_DIR])])
+
+    # Check for Apple Silicon
+    case $host_cpu in
+        arm64|aarch64)
+            MACOS_ARM64=yes
+            ;;
+        *)
+            MACOS_ARM64=no
+            ;;
+    esac
+
+    if test "x$MACOS_ARM64" = xyes; then
+        AC_MSG_NOTICE([Building Spinnaker4 adapter for macOS Apple Silicon (ARM64)])
+    fi
+
+    build_spinnaker4=yes
+else
+    build_spinnaker4=no
+    AC_MSG_WARN([Spinnaker SDK 4.x.x not found; Spinnaker4 adapter will not be built])
+fi
+
+AM_CONDITIONAL([BUILD_SPINNAKER4], [test "x$build_spinnaker4" = xyes])
+
+##
+## End Spinnaker SDK configuration
+##
+
+##
 ## Subdirectory configuration
 ##
